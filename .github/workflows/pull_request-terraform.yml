name: Terraform Plan on PR

on:
  pull_request:
<<<<<<< HEAD
    branches:
      - main
    paths:
      - '**/*.tf'
      - '.github/workflows/pull_request-terraform.yml'
      - 'environments/**'
  push:
    branches:
      - main
    paths:
      - '**/*.tf'
      - 'environments/**'
=======
    branches: [main]
  push:
    branches: [main]
>>>>>>> 1ed0420ddcf45318bb70c84390aff3f306088fd3
  workflow_dispatch:
    inputs:
      vpc_id:
        description: "Existing VPC ID"
        required: true
        type: string
      ec2-name:
        description: "EC2 instance name"
        required: true
        default: 'App Server'
        type: string

permissions:
  id-token: write
  contents: read
<<<<<<< HEAD
  pull-requests: write

env:
  AWS_REGION: ap-south-1
  TERRAFORM_VERSION: 1.6.6
=======
  pull-requests: write  # Needed for PR comments
>>>>>>> 1ed0420ddcf45318bb70c84390aff3f306088fd3

jobs:
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    outputs:
      plan-exitcode: ${{ steps.plan.outputs.exitcode }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ secrets.AWS_IAM_ROLE }}
          role-session-name: terraform-plan-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
          terraform_wrapper: true

      - name: Terraform Init
        id: init
        run: terraform init

      - name: Terraform Format Check
        id: fmt
        run: terraform fmt -check -recursive

      - name: Terraform Validate
        id: validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        run: terraform plan -out=tfplan -input=false
        continue-on-error: true

      - name: Comment Plan on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const plan = `${{ steps.plan.outputs.stdout }}`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## Terraform Plan\n\`\`\`\n${plan}\n\`\`\``
            });

  terraform-apply:
    name: Terraform Apply
    needs: terraform-plan
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
<<<<<<< HEAD
        uses: aws-actions/configure-aws-credentials@v4
=======
        uses: aws-actions/configure-aws-credentials@v5
>>>>>>> 1ed0420ddcf45318bb70c84390aff3f306088fd3
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ secrets.AWS_IAM_ROLE }}
<<<<<<< HEAD
          role-session-name: terraform-apply-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
=======

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3  # Update to latest
>>>>>>> 1ed0420ddcf45318bb70c84390aff3f306088fd3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform Init
        run: terraform init

<<<<<<< HEAD
      - name: Terraform Apply
        run: terraform apply -auto-approve -input=false
=======
      - name: Terraform Format
        run: terraform fmt -check

      - name: Terraform Plan
        uses: dflook/terraform-plan@v1  # Posts plan as PR comment
        with:
          path: .  # Or your tf dir
          token: ${{ secrets.GITHUB_TOKEN }}
          # Add workspace or var inputs if needed

  terraform-apply:
    needs: terraform-plan  # Run after successful plan
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      # Repeat checkout, AWS config, setup, init steps here
      - name: Terraform Apply
        run: |
          terraform plan -input=false -out=tfplan
          terraform apply -auto-approve tfplan
>>>>>>> 1ed0420ddcf45318bb70c84390aff3f306088fd3
